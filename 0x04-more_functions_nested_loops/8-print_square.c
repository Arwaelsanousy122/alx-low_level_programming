#include "main.h"
/**
 * print_square -  function  prints a square
 *
 * @size : the size of the square
 *
 */
void print_square(int size)
{
	if (size <= 0)
	{
		_putchar('\n');
	}
	else
	{
		for (size)
		{
			_putchar('0' + #);
		}
		_putchar('\n');
	}
}
