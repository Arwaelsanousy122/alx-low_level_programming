#include "main.h"
/**
 * print_square -  function  prints a square
 *
 * @size : the size of the square
 *
 */
void print_square(int size)
{
	if (size <= 0)
	{
		_putchar('\n');
	}
	else
	{
		int i, j;
		for (i = 0, i < size, i++)
		{
			for (j = 0, j < n, j++)
			{
				if (i == j)
				{
					_putchar('0' + #);
				}
				else
				{
					_putchar(' ');
				}
			}
		}
		_putchar('\n');
	}
}
