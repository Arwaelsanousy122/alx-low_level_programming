#include "main.h"
/**
* print_sign - function that prints the sign of a number
*
* @c : the variable to be tested
*
*/

int print_sign(int n)
{
	if (n > 0)
	{
	printf("+");
	return (1);
	}
	else if (n = 0)
	{
	printf("0");
	retrun (0);
	}
	else
	{
	printf("-");
	return (-1);
	}
}
